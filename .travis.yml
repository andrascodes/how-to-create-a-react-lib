language: node_js

node_js:
  - 12

cache:
  directories:
    - node_modules
    - dist
    - example/build
    - example/node_modules

jobs:
  include:
    - stage: test
      script:
        - yarn typecheck
        - yarn lint
        - yarn build

    - stage: release
      if: branch = master
      script:
        - yarn build
        - yarn run semantic-release
      deploy:
        provider: pages
        github_token: $github_token
        local_dir: example/build
